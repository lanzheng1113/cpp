**B-Tree：** m阶的B树具有下列特性的多叉搜索树：
> (1) 根节点至少有两个子树，除非它是一个叶节点
> (2) 每个非根非叶节点都有k-1个键值和k个指向子树的指针
        其中键的数目k-1的上下界为:[$\left \lceil  m/2 \right \rceil-1 ,m-1$] ,例如5阶的B-Tree,键数目为[2,4]。
> (3) 每个叶节点都含有k-1个键值,k-1需要满足上下界要求
> (4) 所有的叶节点在同一层

B-Tree的阶，可以理解为孩子节点的最大数目。
