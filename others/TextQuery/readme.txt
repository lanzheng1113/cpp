任务：
设计一个简单的文本查询程序。
要求：
1)它允许用户指明要处理的文件名字。
程序将存储该文件的内容，以便输出每个单词所在的原始行。
2)它必须将每一行分解为每个单词，并记录每个单词所在的所有行。
在输出行号时，应保证升序输出，并且不重复。
3)对特定单词的查询将返回出现该单词的所有行的行号。
4)输出某单词所在的行文本，程序必须能够根据给定的行号从输入文件中获取相应的行。

